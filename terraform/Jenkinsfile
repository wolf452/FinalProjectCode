pipeline {
    agent any

    stages {
        stage('Checkout Code') {
            steps {
                git branch: 'main', url: 'https://github.com/AbdallahHesham44/CloudDevOpsProject.git'
            }
        }

        stage('Terraform Init') {
            steps {
                dir('terraform') {
                    withCredentials([aws(credentialsId: 'aws-cred', region: 'us-east-1')]) {
                        script {
                            env.TF_VAR_AWS_ACCESS_KEY = env.AWS_ACCESS_KEY_ID
                            env.TF_VAR_AWS_SECRET_KEY = env.AWS_SECRET_ACCESS_KEY
                            env.TF_VAR_ ANSIBLE_PRIVATE_KEY = credentials('ansible-ssh-key') 


                            sh 'terraform init'
                        }
                    }
                }
            }
        }

        stage('Terraform Apply') {
            steps {
                dir('terraform') {
                    withCredentials([aws(credentialsId: 'aws-cred', region: 'us-east-1')]) {
                        sh 'terraform apply -auto-approve'
                    }
                }
            }
        }

        stage('Sleep for 3 Minutes') {
            steps {
                script {
                    echo 'Waiting for resources to initialize...'
                    sleep(time: 3, unit: 'MINUTES')
                }
            }
        }

        stage('Get EC2 IP') {
            steps {
                dir('terraform') {
                    script {
                        //def ec2_ip = sh(script: 'terraform output -raw ec2_public_ip', returnStdout: true).trim()
                        
                dir('/var/lib/jenkins/workspace/terr/ansible'){
                    // Create Ansible inventory file dynamically
                    writeFile file: 'inventory', text: """
                   
                    [web]\n${publicIp} ansible_user=ubuntu ansible_ssh_private_key_file=${ansible-ssh-key}
                        
                    """                 
                    }
}
                }
            }
        }
}

        stage('Run Ansible') {
            steps {
                dir('ansible') {
                    ansiblePlaybook credentialsId: 'ansible-ssh-key', inventory: 'inventory', playbook: 'playbook.yml'
                }
            }
        }
    }

    post {
        always {
            archiveArtifacts artifacts: 'inventory, terraform/terraform.tfstate', fingerprint: true
            cleanWs()
        }
    }
}
