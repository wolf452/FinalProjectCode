plugins {
    id 'org.springframework.boot' version '2.6.3'
    id 'io.spring.dependency-management' version '1.0.11.RELEASE'
    id 'java'
    id 'org.sonarqube' version '3.3'  // إضافة SonarQube plugin
}



repositories {
    mavenCentral()
}

dependencies {
    implementation 'org.springframework.boot:spring-boot-starter-web'
    implementation 'org.springframework.boot:spring-boot-starter-thymeleaf' // إذا كنت تستخدم Thymeleaf
    testImplementation('org.springframework.boot:spring-boot-starter-test')
    implementation 'org.springframework.boot:spring-boot-starter'
    testImplementation 'org.springframework.boot:spring-boot-starter-test'
}

test {
    useJUnitPlatform()
}

// تكوين إعدادات SonarQube
sonarqube {
    properties {
        property "sonar.projectKey", "sonar-server"  // اسم المشروع الخاص بك
        property "sonar.host.url", "http://3.86.238.121:9000"  // عنوان SonarQube
        property "sonar.login", "squ_246da60afdc01f831865f05a29e51b0490d90133"  // يمكن استبداله بـ token إذا كنت تستخدم واحدًا
       
    }
}
